---
const year = new Date().getFullYear()

const footerLinks = [
  { text: 'About', url: '/about/' },
  { text: 'Services', url: '/services/' },
  { text: 'Articles', url: '/articles/' },
  { text: 'Blog', url: '/blog/' },
]
---

<footer class="border-t border-slate-200 bg-white">
  <div class="mx-auto flex max-w-6xl flex-col gap-6 px-4 py-10 md:flex-row md:items-center md:justify-between">
    <div>
      <p class="text-sm font-semibold text-slate-900">MediaPath EU</p>
      <p class="text-sm text-slate-600">Performance marketing for iGaming growth in Europe.</p>
    </div>

    <nav aria-label="Footer" class="flex flex-wrap gap-4">
      {
        footerLinks.map((link) => (
          <a href={link.url} class="text-sm font-medium text-slate-600 transition-colors hover:text-slate-900">
            {link.text}
          </a>
        ))
      }
    </nav>

    <p class="text-sm text-slate-500">© {year} MediaPath EU</p>
  </div>
</footer>
